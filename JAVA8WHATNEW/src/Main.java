// JAVA 8 một phiên bản mới, có các nâng cấp mới
// default method: cho phép thêm các method không trừu tượng bằng cách thêm từ khoá default
// lambda expression => viết code ít hơn, thay vì phải override run
// stream API
// date time API: cung cấp bố xử lí tối ưu cho các định dạng thời gian
// optional class: Optional<T> để kiểm tra xem một biến có giá trị tồn tại hay không, tác dụng: check giá trị,
// tránh lỗi NullPointerException lúc runtime (khi gọi đến biến chưa dược khởi tạo hoặc gán null)
// ví dụ về optional

/* class Student {
String name;
}
public class OptionalEx () {
Student student=new Student();
Optional <student> op=new Optional.of(student);
if (op.isPresent()) {
system.out.println(op.get().name)
}

}




 */

public class Main {

}